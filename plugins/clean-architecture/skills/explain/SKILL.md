---
name: explain
description: Explain code, errors, diffs, changes, systems, or the current session in plain language after reading the evidence. Use when a user asks what happened, why something works, what an error means, or invokes `$explain` as the Codex equivalent of `/explain`.
---

# Explain

Explain the requested target with the shared explain command.

## Run the command

1. Resolve `../../commands/explain.md` from this skill directory.
2. Read the command file completely before writing the explanation.
3. Load the `clean-writing` skill as the command requires.
4. Replace `$ARGUMENTS` with the request text that follows the skill invocation.
5. Use Codex read-only tools to inspect the evidence before explaining it.

Treat the shared command as the source of truth for target selection, structure, and writing rules.
