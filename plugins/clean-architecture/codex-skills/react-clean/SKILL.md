---
name: react-clean
description: Rules for writing clean React components. Use before writing or editing any React component, hook, or `.tsx` file. Builds on the `ts-clean` skill; follow that one too.
---

# react-clean

The rules live in one shared skill. This entry point points at it.

Read `../../skills/react-clean/SKILL.md` completely before you write or edit a React component or hook, and follow every rule in it.
Read every reference that file requires, and resolve each path against `../../skills/react-clean/`.
