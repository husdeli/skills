---
name: ts-clean
description: Rules for writing clean TypeScript in any file. Use before writing or editing any `.ts` or `.tsx` file — a service, a domain module, a hook, a utility, or a config file.
---

# ts-clean

The rules live in one shared skill. This entry point points at it.

Read `../../skills/ts-clean/SKILL.md` completely before you write or edit a `.ts` or `.tsx` file, and follow every rule in it.
Read every reference that file requires, and resolve each path against `../../skills/ts-clean/`.
