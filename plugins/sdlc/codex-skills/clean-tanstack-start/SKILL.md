---
name: clean-tanstack-start
description: Rules for organizing and securing TanStack Start server functions and server-only code. Use before editing any file that defines, imports, or calls `createServerFn`, or that reaches a database or a secret. Builds on the `ts-clean` skill; follow that one too.
---

# clean-tanstack-start

The rules live in one shared skill. This entry point points at it.

Read `../../skills/clean-tanstack-start/SKILL.md` completely before you edit a TanStack Start server function or server-only module, and follow every rule in it.
Read every reference that file requires, and resolve each path against `../../skills/clean-tanstack-start/`.
